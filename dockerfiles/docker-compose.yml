version: '3.8'

services:
  revele-ai:
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    container_name: revele-ai-app
    restart: unless-stopped

